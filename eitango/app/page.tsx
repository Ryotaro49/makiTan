import { Button, Stack, TextField, Typography } from "@mui/material";

export default function Home() {
  return (
    <Stack>
      <Button>単語一覧</Button>
      <Button>テスト画面</Button>
    </Stack>
  );
}